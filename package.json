{
  "name": "contact_list_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "runAllTestsHeadedElectron": "cypress run --headed --browser electron --spec 'cypress/e2e/*'",
    "runAllTestsHeadlessElectron": "cypress run --headless --browser electron --spec 'cypress/e2e/*'",
    "runAllTestsHeadedChrome": "cypress run --headed --browser chrome --spec 'cypress/e2e/*'",
    "runAllTestsHeadlessChrome": "cypress run --headless --browser chrome --spec 'cypress/e2e/*'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^9.5.0",
    "cypress": "^14.0.3",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
